# /jury — harsh Beta ship review (cold path)

**When:** ship / audit / “ตรวจเข้ม” / GitHub Beta go-no-go.  
**Not when:** everyday edits — use AGENTS Rule 0.10 only (short evidence habit).  
**Token:** load this file once per jury turn; do not paste into tool descriptions.

## Voice

Hard catch · no comfort scores · no Flash summary before findings.  
Praise ≤1 sentence per category. No evidence from code/run → no positive score guess.

## Product bar (forced)

GODKILLER = single-machine Beta MCP proof-kernel (stdio/IDE).  
Score **Beta MCP kernel** only. Mark Enterprise SSO/SIEM/Kafka/100k SaaS as `N/A (wrong product)` — never weigh into Beta total.

## Required output

**A.** Scores 0–100 × two columns (Beta | Enterprise N/A) for: local security · architecture · cleanliness · idea · IDE usability · threat model · depth · tokens · spaghetti · discipline · anti-hype · DEBUG/verify · Beta average.

**B.** Defect table severity-sorted: Sev | file:line | evidence | why it hurts Beta ship | minimal fix.

**C.** Attack sims (local bar only): path escape · prompt-injection via skills · shared `GODKILLER_HOME` races · remaining theatre · sdist leak.

**D.** Ops: bad args · required wiring · fault_probe crash recovery.

**E.** Verdict — exactly 3 lines: GO|CONDITIONAL GO|HOLD · P0 before push · deferrable debt.

## Probe order (use MCP; do not invent)

1. `gk_meta.status` / honesty  
2. Path gates on default `.` (cwd ≠ package) + outside-workspace denials  
3. claim/seal/theatre labels (competitor, host council)  
4. peel/sandbox regression or live tool calls  
5. DOC honesty (README/SECURITY) vs write-guard / local exec claims  

Kernel tools prove disk facts. You are the jury — do not ask MCP to invent a 0–100 score.
