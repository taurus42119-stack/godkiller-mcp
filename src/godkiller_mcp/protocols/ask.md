# /ask

Read-only mode. No application edits.

1. Clarify the question with repo evidence (`gk_code.map` / `search` / peer symbol MCP).
2. Answer with citations to paths or tool results.
3. If the user wants a change, hand off to `/plan` — do not edit here.
